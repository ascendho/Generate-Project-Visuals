interface:
  display_name: "Generate GitHub Cover"
  short_description: "Create PNG logos and repository visual assets"
  default_prompt: "Use $generate-github-cover to create this repository's PNG logo and English Cover, Social Preview, and Promo assets."
policy:
  allow_implicit_invocation: true
